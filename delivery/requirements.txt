# Quran ASR Kids API — Python Dependencies
# Install: pip install -r requirements.txt
# Note: Install PyTorch first with CUDA support if you have a GPU:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121

# --- API Server ---
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
httpx>=0.27.0

# --- AI / ML ---
# Single model = NAMAA-Space/Cohere-Speech-Tashkeel-2B (CohereAsrForConditionalGeneration):
# outputs the learner's ACTUAL diacritized recitation (words + harakat), enabling both
# word and harakat grading. Needs transformers>=5.4, bf16 GPU (~5 GB). Do NOT downgrade
# transformers below 5.4.
torch>=2.4.0
transformers>=5.4.0
accelerate>=1.0.0
sentencepiece>=0.2.0
protobuf>=4.0.0
safetensors>=0.4.0

# --- Audio Processing ---
librosa>=0.10.1
soundfile>=0.12.1
numpy>=1.24.0
scipy>=1.11.0

# --- Text Processing ---
jiwer>=3.0.3

# --- TTS (Text-to-Speech) ---
edge-tts>=6.1.9

# --- Utilities ---
requests>=2.31.0
moviepy>=1.0.3
